A python package providing plotting facilities customized for spike sorting.
The package uses the matplotlib package for plotting. It can be used with a gui
event loop for online display or a file backend for use in environments where
no display is available, like a webserver.
